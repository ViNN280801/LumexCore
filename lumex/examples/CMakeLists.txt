# lumex/examples/CMakeLists.txt

set_property(GLOBAL PROPERTY LUMEX_EXAMPLE_TARGETS "")

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_BASE64)
    add_subdirectory(base64)
else()
    message(STATUS "Skipping base64 examples (LUMEX_BUILD_CORE or LUMEX_BUILD_BASE64 is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_CIRCULAR_BUFFER)
    add_subdirectory(circular_buffer)
else()
    message(STATUS "Skipping circular_buffer examples (LUMEX_BUILD_CORE or LUMEX_BUILD_CIRCULAR_BUFFER is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_CRC)
    add_subdirectory(crc)
else()
    message(STATUS "Skipping crc examples (LUMEX_BUILD_CORE or LUMEX_BUILD_CRC is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_ENVIRONMENT)
    add_subdirectory(environment)
else()
    message(STATUS "Skipping environment examples (LUMEX_BUILD_CORE or LUMEX_BUILD_ENVIRONMENT is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_EXCEPTIONS)
    add_subdirectory(exceptions)
else()
    message(STATUS "Skipping exceptions examples (LUMEX_BUILD_CORE or LUMEX_BUILD_EXCEPTIONS is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_EXPECTED)
    add_subdirectory(expected)
else()
    message(STATUS "Skipping expected examples (LUMEX_BUILD_CORE or LUMEX_BUILD_EXPECTED is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_FILESYSTEM)
    add_subdirectory(filesystem)
else()
    message(STATUS "Skipping filesystem examples (LUMEX_BUILD_CORE or LUMEX_BUILD_FILESYSTEM is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_GENERATORS)
    add_subdirectory(generators)
else()
    message(STATUS "Skipping generators examples (LUMEX_BUILD_CORE or LUMEX_BUILD_GENERATORS is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_MATH)
    add_subdirectory(math)
else()
    message(STATUS "Skipping math examples (LUMEX_BUILD_CORE or LUMEX_BUILD_MATH is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_OPTIONAL)
    add_subdirectory(optional)
else()
    message(STATUS "Skipping optional examples (LUMEX_BUILD_CORE or LUMEX_BUILD_OPTIONAL is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_REFLECTION)
    add_subdirectory(reflection)
else()
    message(STATUS "Skipping reflection examples (LUMEX_BUILD_CORE or LUMEX_BUILD_REFLECTION is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_STRING)
    add_subdirectory(string)
else()
    message(STATUS "Skipping string examples (LUMEX_BUILD_CORE or LUMEX_BUILD_STRING is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_STRING_VIEW)
    add_subdirectory(string_view)
else()
    message(STATUS "Skipping string_view examples (LUMEX_BUILD_CORE or LUMEX_BUILD_STRING_VIEW is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_TEMPORARY)
    add_subdirectory(temporary)
else()
    message(STATUS "Skipping temporary examples (LUMEX_BUILD_CORE or LUMEX_BUILD_TEMPORARY is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_TIME)
    add_subdirectory(time)
else()
    message(STATUS "Skipping time examples (LUMEX_BUILD_CORE or LUMEX_BUILD_TIME is OFF)")
endif()

if(LUMEX_BUILD_CORE AND LUMEX_BUILD_UTILITY)
    add_subdirectory(utility)
else()
    message(STATUS "Skipping utility examples (LUMEX_BUILD_CORE or LUMEX_BUILD_UTILITY is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_HARDWARE)
    add_subdirectory(hardware)
else()
    message(STATUS "Skipping hardware examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_HARDWARE is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_JSON)
    add_subdirectory(json)
else()
    message(STATUS "Skipping json examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_JSON is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_LOGGER)
    add_subdirectory(logger)
else()
    message(STATUS "Skipping logger examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_LOGGER is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_LOGGING)
    add_subdirectory(logging)
else()
    message(STATUS "Skipping logging examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_LOGGING is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_RESOURCE_MONITOR)
    add_subdirectory(resource_monitor)
else()
    message(STATUS "Skipping resource_monitor examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_RESOURCE_MONITOR is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_SERIAL)
    add_subdirectory(serial)
else()
    message(STATUS "Skipping serial examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_SERIAL is OFF)")
endif()

if(LUMEX_BUILD_APPLIED AND LUMEX_BUILD_SETTINGS)
    add_subdirectory(settings)
else()
    message(STATUS "Skipping settings examples (LUMEX_BUILD_APPLIED or LUMEX_BUILD_SETTINGS is OFF)")
endif()

if(LUMEX_BUILD_XML)
    add_subdirectory(xml)
else()
    message(STATUS "Skipping xml examples (LUMEX_BUILD_XML=OFF)")
endif()

get_property(_example_targets GLOBAL PROPERTY LUMEX_EXAMPLE_TARGETS)
if(LUMEX_INSTALL AND _example_targets)
    install(TARGETS ${_example_targets} DESTINATION bin)
endif()

